"use client";

import { FC } from "react";

const ReviewSection: FC = () => {
    return (
      <section className="flex flex-row text-center align-middle justify-center bg-base-colors-50">
        Placeholder
      </section>
    );
};

export default ReviewSection;